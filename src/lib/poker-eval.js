// Returns { score, name, kickerRanks, bestFive }
export function evaluateBestHand(cards) {
  // (use the evaluator from the canvas; paste it here unchanged)
}

export function compareHands(hA, hB) {
  return hA.score - hB.score; // >0 = A better, <0 = B better, 0 = tie
}
